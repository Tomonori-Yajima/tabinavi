<% if @post.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      以下の<%= pluralize(@post.errors.count, "つ") %>のエラーがあります。
    </div>
    <ul>
    <% @post.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>